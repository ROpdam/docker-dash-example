#From https://towardsdatascience.com/docker-for-python-dash-r-shiny-6097c8998506

version: "3.7"
services:
  dash-example: 
    build: 
      context: ./project
    # command: uvicorn app.app:server --reload --workers 1 --host 0.0.0.0 --port 8050
    # volumes:
    #   - ./project:/usr/src/app
    image: dash-example:$VERSION
    container_name: dash-example
    ports:
      - "8050:8050"
    environment:
      - TARGET=$TARGET
    restart: unless-stopped